spring:
  application:
    name: conta-bancaria-clean-arc
  datasource:
    url: jdbc:h2:mem:conta_bancaria
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true